spring.application.name=research

# Server Configuration
server.port=${PORT:8080}

# Database Connection Settings (Neon PostgreSQL)
spring.datasource.url=${DATABASE_URL:jdbc:postgresql://ep-orange-heart-a19lr623-pooler.ap-southeast-1.aws.neon.tech/neondb?sslmode=require}
spring.datasource.username=${DATABASE_USERNAME:neondb_owner}
spring.datasource.password=${DATABASE_PASSWORD:npg_DmZL2McwuBN6}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate Settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Connection Pooling
spring.datasource.hikari.maximum-pool-size=${HIKARI_MAX_POOL_SIZE:5}
spring.datasource.hikari.minimum-idle=0
spring.datasource.hikari.idle-timeout=30000
spring.datasource.hikari.max-lifetime=600000

# Cloudinary Configuration (Use environment variables for production)
cloudinary.cloud-name=${CLOUDINARY_CLOUD_NAME:}
cloudinary.api-key=${CLOUDINARY_API_KEY:}
cloudinary.api-secret=${CLOUDINARY_API_SECRET:}

# # Gmail SMTP configuration for member password resets
# spring.mail.host=smtp.gmail.com
# spring.mail.port=${MAIL_SMTP_PORT:465}
# spring.mail.username=${GMAIL_SMTP_USERNAME:biochemical.research.lab@gmail.com}
# spring.mail.password=${GMAIL_APP_PASSWORD:dneklwzqgxikbtkz}
# spring.mail.properties.mail.smtp.auth=true
# spring.mail.properties.mail.smtp.ssl.enable=${MAIL_SMTP_SSL:true}
# spring.mail.properties.mail.smtp.starttls.enable=${MAIL_SMTP_STARTTLS:false}
# spring.mail.properties.mail.smtp.starttls.required=${MAIL_SMTP_STARTTLS:false}
# spring.mail.properties.mail.smtp.ssl.trust=smtp.gmail.com
# spring.mail.properties.mail.debug=${MAIL_SMTP_DEBUG:false}
# spring.mail.properties.mail.smtp.connectiontimeout=5000
# spring.mail.properties.mail.smtp.timeout=5000
# spring.mail.properties.mail.smtp.writetimeout=5000
# spring.mail.properties.mail.smtp.from=${spring.mail.username}

# mail.sender.name=${MAIL_SENDER_NAME:Biochemical and Environmental Research Lab}
# mail.sender.email=${MAIL_SENDER_EMAIL:${spring.mail.username}}

############################################################
# Gmail SMTP Configuration for Member Password Resets
############################################################

spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:465}

spring.mail.username=${MAIL_USERNAME:berl.cuet@gmail.com}
spring.mail.password=${MAIL_PASSWORD:trlyxgahnvqltnoe}

spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.ssl.enable=true
spring.mail.properties.mail.smtp.ssl.trust=smtp.gmail.com
spring.mail.properties.mail.smtp.socketFactory.port=465
spring.mail.properties.mail.smtp.socketFactory.class=javax.net.ssl.SSLSocketFactory
spring.mail.properties.mail.smtp.timeout=30000
spring.mail.properties.mail.smtp.connectiontimeout=30000
spring.mail.properties.mail.smtp.writetimeout=30000
spring.mail.properties.mail.debug=${MAIL_SMTP_DEBUG:true}

# Default From Address
spring.mail.properties.mail.smtp.from=${spring.mail.username}

############################################################
# Sender Information
############################################################

mail.sender.name=${MAIL_SENDER_NAME:Biochemical and Environmental Research Lab}
mail.sender.email=${MAIL_SENDER_EMAIL:${spring.mail.username}}

# File Upload Configuration
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Production settings
spring.jpa.open-in-view=false